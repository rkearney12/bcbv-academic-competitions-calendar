// Paste the Google Sheet's published CSV URL between the quotation marks.
// Google Sheets: File → Share → Publish to web → choose the Competitions tab
// and "Comma-separated values (.csv)", then copy the generated link.
export const GOOGLE_SHEET_CSV_URL = "";

